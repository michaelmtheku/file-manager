# App config
PORT=5000
NODE_ENV=development

# Metadata file (used by tests via METADATA_FILE)
METADATA_FILE=metadata.json

# Uploads directory (optional)
UPLOAD_DIR=uploads

# JWT secret (must be set in production / CI)
JWT_SECRET=change-me-in-production

# Optional: app version
APP_VERSION=0.1.0
